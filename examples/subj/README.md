Example generation run (subj, gemini-2.0-flash, soft labels + CoT).
`data.jsonl` is truncated to the first 200 of 20042 records to keep the
repository small; `config.yaml` shows the full run configuration.
