Example generation run (sst, gemini-2.0-flash, soft labels + CoT).
`data.jsonl` is truncated to the first 200 of 20049 records to keep the
repository small; `config.yaml` shows the full run configuration.
